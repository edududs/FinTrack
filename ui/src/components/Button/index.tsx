import { Button, buttonVariants, ButtonProps } from "./ButtonComponent";
import { BUTTON_VARIANTS, BUTTON_SIZES } from "./ButtonTypes";


export {
  Button,
  buttonVariants,
  type ButtonProps,
  BUTTON_VARIANTS,
  BUTTON_SIZES,
};

export default Button;